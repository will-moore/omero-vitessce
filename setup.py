from setuptools import setup, find_packages

setup(
    name='omero-vitessce',
    version='0.0.1',
    description="OMERO.web plugin for Vitessce",
    packages=find_packages(),
    keywords=['omero', 'vitessce'],
)